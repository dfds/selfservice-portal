import {
  BasicAsyncSelectField,
  DisabledAsyncSelectField,
  LabelAsyncSelectField,
  SizesAsyncSelectField,
  HelpAsyncSelectField,
  ClearableAsyncSelectField,
  AssistiveAsyncSelectField,
} from './AsyncSelectField.stories'
import { AsyncSelectField } from './AsyncSelectField'
import { Meta, DocsContainer, Story, Props, Preview, withWidth } from '@/dfds-ui/storybook-design'

<Meta title="Hydro UI/Forms/AsyncSelectField" component={{ AsyncSelectField }} />

# AsyncSelectField

```jsx
import { AsyncSelectField } from '@/dfds-ui/forms/src'
```

<Preview width="320px" gray>
  <Story story={BasicAsyncSelectField} />
</Preview>

## Component props

<Props of={AsyncSelectField} />

## Label

Use the `label` prop to specify the label. Please notice, the label size is based on visualSize prop.

<Preview width="320px" gray>
  <Story story={LabelAsyncSelectField} />
</Preview>

## Disabled

Use the `disabled` prop

<Preview width="320px" gray>
  <Story story={DisabledAsyncSelectField} />
</Preview>

## Sizes

Use the `visualSize` prop to specify the size variant.

<Preview width="320px" gray>
  <Story story={SizesAsyncSelectField} />
</Preview>

## AssistiveText

Use the `assistiveText` prop to specify text.

<Preview width="320px" gray>
  <Story story={AssistiveAsyncSelectField} />
</Preview>

## Help Message

Use the `help` prop to specify a help message.

<Preview width="320px" gray>
  <Story story={HelpAsyncSelectField} />
</Preview>

## Using `isClearable`

Use the `isClearable` prop if you want the user to be able to clear the selection.

<Preview width="320px" gray>
  <Story story={ClearableAsyncSelectField} />
</Preview>
