import { Meta, DocsContainer, Story, Props, Preview, withWidth } from '@/dfds-ui/storybook-design'
import { PasswordField } from './PasswordField'
import { BasicPasswordField } from './PasswordField.stories'

<Meta title="Hydro UI/Forms/PasswordField" component={{ PasswordField }} />

# PasswordField

Component for displaying password input. It work like a `TextField` component but without the `type` prop.

```jsx
import { PasswordField } from '@/dfds-ui/forms/src'
```

<Preview width="320px" gray>
  <Story name="Basic" story={BasicPasswordField} />
</Preview>

## Component props

<Props of={PasswordField} />
