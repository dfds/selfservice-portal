import Step from './Step'

import {
  Meta,
  Props,
  DocsContainer,
  Story,
  ArgsTable,
  Preview,
  Description,
  withWidth,
} from '@dfds-ui/storybook-design'

<Meta title="Hydro UI/Stepper/Step" component={Step} />

# Step

API documentation for the `Step` component.

The `Step` component is used as a child to the `<Stepper/>` component.

<Description of={Step} />

## Usage

```jsx
import { Step } from '@/components/dfds-ui/react-components'
```

<ArgsTable of={Step} />

## Demo

- Refer to the `<Stepper/>` component.
